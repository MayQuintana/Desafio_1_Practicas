<?php View::content() ?>
<h3>Crear Usuario<h3>

<?= Form::open(); // por defecto llama a la misma url ?> 

<label>ID
      <?= Form::text('usuarios.id') ?></label>
     
      <label>Nombre
      <?= Form::text('usuarios.nombre') ?></label>

    
      <?= Form::submit('Agregar') ?> 

<?= Form::close() ?> 